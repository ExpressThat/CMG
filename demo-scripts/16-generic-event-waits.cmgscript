# Demonstrates provider-style waitForEvent aliases over CMG waits.

openTab "about:blank"
waitForEvent popup count=2 timeout=5000

captureDialogs
onDialog accept
evaluate "alert('Saved from waitForEvent')"
waitForEvent dialog "Saved from waitForEvent" timeout=5000
