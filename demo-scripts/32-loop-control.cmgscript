navigate "index.html"
set ready false

repeat i 4 {
  if (${i} == 1) {
    continue
  }

  if (${i} == 3) {
    break
  }

  caption "Repeat ${i}"
}

while (${ready} == false) max=3 {
  set ready true
  caption "Ready"
}
