# `browser control page runtime locatorCount`

Alias for [`count`](count.md).

```powershell
cmg browser control page runtime locatorCount "<selector>"
```

Stdout, stderr, arguments, and exit codes match `count`.
