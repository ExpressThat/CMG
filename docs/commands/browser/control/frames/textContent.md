# `browser control frames textContent`

Alias for [`frameTextContent`](frameTextContent.md).

```powershell
cmg browser control frames textContent "<frameSelector>" "<selector>"
```

Stdout, stderr, exit codes, and examples match [`frameTextContent`](frameTextContent.md).
