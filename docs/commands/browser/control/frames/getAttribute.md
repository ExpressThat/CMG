# `browser control frames getAttribute`

Alias for [`frameGetAttribute`](frameGetAttribute.md).

```powershell
cmg browser control frames getAttribute "<frameSelector>" "<selector>" "<name>"
```

Stdout, stderr, exit codes, and examples match [`frameGetAttribute`](frameGetAttribute.md).
