# `browser control frames innerText`

Alias for [`frameInnerText`](frameInnerText.md).

```powershell
cmg browser control frames innerText "<frameSelector>" "<selector>"
```

Stdout, stderr, exit codes, and examples match [`frameInnerText`](frameInnerText.md).
