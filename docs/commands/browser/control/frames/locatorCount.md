# `browser control frames locatorCount`

Alias for [`frameLocatorCount`](frameLocatorCount.md).

```powershell
cmg browser control frames locatorCount "<frameSelector>" "<selector>"
```

Stdout, stderr, exit codes, and examples match [`frameLocatorCount`](frameLocatorCount.md).
