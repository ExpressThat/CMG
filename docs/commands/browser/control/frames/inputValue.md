# `browser control frames inputValue`

Alias for [`frameInputValue`](frameInputValue.md).

```powershell
cmg browser control frames inputValue "<frameSelector>" "<selector>"
```

Stdout, stderr, exit codes, and examples match [`frameInputValue`](frameInputValue.md).
