# `browser control frames frameLocatorCount`

Alias for [`frameCount`](frameCount.md).

```powershell
cmg browser control frames frameLocatorCount "<frameSelector>" "<selector>"
```

Stdout, stderr, exit codes, and examples match [`frameCount`](frameCount.md).
