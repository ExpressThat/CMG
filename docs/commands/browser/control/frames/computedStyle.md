# `browser control frames computedStyle`

Alias for [`frameComputedStyle`](frameComputedStyle.md).

```powershell
cmg browser control frames computedStyle "<frameSelector>" "<selector>" "<property>"
```

Stdout, stderr, exit codes, and examples match [`frameComputedStyle`](frameComputedStyle.md).
