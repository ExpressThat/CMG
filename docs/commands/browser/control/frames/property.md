# `browser control frames property`

Alias for [`frameProperty`](frameProperty.md).

```powershell
cmg browser control frames property "<frameSelector>" "<selector>" "<name>"
```

Stdout, stderr, exit codes, and examples match [`frameProperty`](frameProperty.md).
